Place the official SMPA emblem in this folder (e.g. smpa-logo.png or
smpa-logo.svg), then set `site.logoSrc` in src/config/routeConfig.ts to its
path, e.g. '/smpa-logo.png'.

Until a file is supplied the header renders a neutral placeholder. Do not
substitute an unofficial or reconstructed emblem.
